[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run serve"
buildCommand = "npm run build"

# Volume configuration for persistent uploads
[[volumes]]
name = "uploads"
mountPath = "/app/uploads"
