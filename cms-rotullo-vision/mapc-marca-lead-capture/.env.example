# Exemplo de arquivo .env.local para produção
# ATENÇÃO: NÃO inclua este arquivo no repositório!
# Renomeie este arquivo para .env.local após configurar os valores

# ============================================
# CONFIGURAÇÃO WEBHOOK: Formspree → Krayin CRM  
# ============================================

# Formspree (PRINCIPAL - funcionando) ✅
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/xrbyzrog

# ============================================
# Krayin CRM via Netlify Function (SOLUÇÃO CORS)
# Configurar no painel do Netlify como Environment Variables
# ============================================

# KRAYIN_API_BASE_URL=https://crm.mapc.com.br/api/v1
# KRAYIN_ADMIN_EMAIL=admin@mapc.com.br
# KRAYIN_ADMIN_PASSWORD=sua_senha_admin

# ============================================
# Krayin CRM (DESABILITADO - erro 500)
# Descomente quando o servidor for corrigido
# ============================================

# VITE_KRAYIN_API_BASE_URL=https://crm.mapc.com.br/api/v1

# Opção 1: Token direto (recomendado para produção)
# VITE_KRAYIN_API_TOKEN=Bearer_SEU_TOKEN_AQUI

# Opção 2: Credenciais para gerar token automaticamente
# VITE_KRAYIN_ADMIN_EMAIL=admin@mapc.com.br
# VITE_KRAYIN_ADMIN_PASSWORD=sua_senha_admin

# Lead Configuration (para quando CRM voltar)
# VITE_LEAD_SOURCE_ID=3
# VITE_LEAD_TYPE_ID=1
# VITE_DEFAULT_USER_ID=1

# Webhook Configuration (opcional)
# VITE_N8N_WEBHOOK_URL=https://n8n-mapc.seu-servidor.com/webhook/novo-lead
